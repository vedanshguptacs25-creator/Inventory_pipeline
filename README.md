# Inventory Restock Pipeline

This project predicts stock-out and generates restocking recommendations.

## Features
- CSV data ingestion
- Data processing using pandas
- MySQL database storage
- JSON output generation
- Logging and error handling

## How to run
1. Install dependencies:
   pip install pandas mysql-connector-python

2. Run:
   python inventory_pipeline.py# Inventory_pipeline
